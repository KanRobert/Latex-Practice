% ------------------------------------------------------------------------
% file `C++Primer_en-exercises-40-exercise-body.tex'
%
%     exercise of type `exercises' with id `40'
%
% generated by the `exercises' environment of the
%   `xsim' package v0.8a (2017/05/19)
% from source `C++Primer_en' on 2018/01/04 on line 6492
% ------------------------------------------------------------------------
\begin{question}
Given that \verb|p1| and \verb|p2| point to elements in the same array,
what does the following code do? Are there values of \verb|p1| or \verb|p2| that make
this code illegal?
\begin{lstlisting}
p1 += p2 - p1;
\end{lstlisting}
\end{question}

\begin{question}
Using pointers, write a program to set the elements in an array to zero.
\end{question}

\begin{question}
Write a program to compare two arrays for equality. Write a similar program to compare two \verb|vector|s.
\end{question}
