% ------------------------------------------------------------------------
% file `C++Primer_en-exercises-85-exercise-body.tex'
%
%     exercise of type `exercises' with id `85'
%
% generated by the `exercises' environment of the
%   `xsim' package v0.8a (2017/05/19)
% from source `C++Primer_en' on 2017/12/23 on line 13017
% ------------------------------------------------------------------------
\begin{question}
Given the following declarations,
\begin{lstlisting}
void manip(int, int);
double dobj
\end{lstlisting}
what is the rank (\S~\ref{subsec:argument type conversions}, p. \pageref{subsec:argument type conversions}) of each conversion in the following calls?
\begin{enumerate}[label=(\alph*)]
^^I\item \verb|manip('a', 'z');|
^^I\item \verb|manip(55.4, dobj);|
\end{enumerate}
\end{question}

\begin{question}
Explain the effect of the second declaration in each one of
the following sets of declarations. Indicate which, if any, are illegal.
\begin{enumerate}[label=(\alph*)]
^^I\item
\begin{lstlisting}
int calc(int&, int&);
int calc(const int&, const int&);
\end{lstlisting}

^^I\item
\begin{lstlisting}
int calc(char*, char*);
int calc(const char*, const char*);
\end{lstlisting}

^^I\item
\begin{lstlisting}
int calc(char*, char*);
int calc(char* const, char* const);
\end{lstlisting}
\end{enumerate}
\end{question}
