% ------------------------------------------------------------------------
% file `C++Primer_en-exercises-98-exercise-body.tex'
%
%     exercise of type `exercises' with id `98'
%
% generated by the `exercises' environment of the
%   `xsim' package v0.8a (2017/05/19)
% from source `C++Primer_en' on 2018/03/12 on line 15085
% ------------------------------------------------------------------------
\begin{question}
What would happen if we put the \verb|typedef| of \verb|pos| in the
\verb|Screen| class on page \pageref{lst:normal block-scope name lookup inside member definitions} as the last line in the class?
\end{question}

\begin{question}
Explain the following code, indicating which definition of
\verb|Type| or \verb|initVal| is used for each use of those names. Say how you would
fix any errors.
\begin{lstlisting}
typedef string Type;
Type initVal();
class Exercise {
public:
   typedef double Type;
   Type setVal(Type);
   Type initVal();
private:
   int val;
};
Type Exercise::setVal(Type parm) {
   val = parm + initVal();
   return val;
}
\end{lstlisting}
\end{question}
